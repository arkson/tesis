<%= form_for(@area_conocimiento) do |f| %>
  <% if @area_conocimiento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@area_conocimiento.errors.count, "error") %> prohibited this area_conocimiento from being saved:</h2>

      <ul>
      <% @area_conocimiento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
