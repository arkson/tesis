<h1>Listing lines_items</h1>

<table>
  <tr>
    <th>Ejemplar</th>
    <th>Cart</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @lines_items.each do |lin_items| %>
  <tr>
    <td><%= lin_items.ejemplar %></td>
    <td><%= lin_items.cart %></td>
    <td><%= link_to 'Show', lin_items %></td>
    <td><%= link_to 'Edit', edit_lin_items_path(lin_items) %></td>
    <td><%= link_to 'Destroy', lin_items, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Line item', new_lin_items_path %>
