<% if notice %>
	<p class="notice"><%= notice %></p>
<% end %>


<div class="cart_title">Tu Alquiler</div>
<table>
  <%= render(cart.line_item) %>

  <tr class="total_line">
    <td colspan="2">Total</td>
    <td class="total_cell"><%= number_to_currency(cart.total_price, :unit => "Bs.") %></td>
  </tr>

</table>
 

<%= button_to "Alquilar", ppal_estudiante_confirmar_alquiler_path, :method => :get, :class => "alquilar" %>
<%= button_to 'Vaciar', cart, :method => :delete, :confirm => '¿Estas seguro?' %>
