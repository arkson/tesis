<%= form_for(@donacion) do |f| %>
  <% if @donacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@donacion.errors.count, "error") %> prohibited this donacion from being saved:</h2>

      <ul>
      <% @donacion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ejemplar %><br />
    <%= f.text_field :ejemplar %>
  </div>
  <div id = "ejemplar_desc"></div>		
  <div class="field">
    <%= f.label :usuario  %><br />
    <%= f.text_field :usuario, :onchange => buscar_usuario_donaciones_path %>
  </div>
  <div id = "usuario_desc"></div>	
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>







