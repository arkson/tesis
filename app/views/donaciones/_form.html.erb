<%= form_for(@donacion) do |f| %>
  <% if @donacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@donacion.errors.count, "error") %> prohibited this donacion from being saved:</h2>

      <ul>
      <% @donacion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
   <%= f.label :ejemplar %><br />
   <%= f.collection_select :ejemplar_id, Ejemplar.joins(:libro).order('libros.cota asc'), :id, :cota %> 
   
    </div>
 
	
  <div class="field">
    <%= f.label :usuario %><br />
	<%= f.collection_select :usuario_id, Usuario.order('cedula asc'), :id, :cedula, :onchange => 'this.form.submit()'  %>
	

  </div>
  <div id = "usuario_des"></div> 	

  <div class="actions">
    <%= f.submit t('.submit') %>
  </div>
<% end %>
