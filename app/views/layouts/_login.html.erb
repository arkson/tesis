

<div class="demo">

<div id="dialog-form" class="login">
	<% if flash[:alert] %>
		<p class="alerta"><%= flash[:alert] %></p>
	<% end %>	

	<%= form_tag url_for (:controller => "sesiones",:action => "crear") do %>
	
		<div>
			<label for="cedula">Cédula:</label><br />
			<%= text_field_tag :cedula, params[:cedula] %>
		</div>
		<div>
			<label for="clave">Clave:</label><br />
			<%= password_field_tag :password, params[:password] %>
		</div>
		<div>
			<label>Texto de la imagen:</label><br />
			<%= show_simple_captcha(:image_style => 'simply_blue', :distortion => 'medium',:label => "")  %>
		</div>
		<div class = "actions">
			<%= submit_tag "Entrar", :id => 'links' %>
		</div>
		<button id="olvido">Olvidó su clave?</button>

	<% end %>
</div>


<div id="dialog-form2" class="login">
	<% if flash[:alert] %>
		<p class="alerta"><%= flash[:alert] %></p>
	<% end %>

	<%= form_tag url_for(:controller => 'inicio', :action => 'olvido_clave')  do   %>
		Por favor introduzca los datos solicitados
		<div>
			<label for="cedula">Cédula:</label><br />
			<%= text_field_tag :cedula, params[:cedula] %>
		</div>
		<div class = "actions">
			<%= submit_tag "Aceptar", :id => 'links' %>
		</div>
	<% end %>

</div>


</div><!-- End demo -->	
