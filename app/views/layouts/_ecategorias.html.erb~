<div style="font-size:12px; background:   #ebf3ff;margin-bottom:2px;">

Catálogo por Escuelas:

	<% @areas.each do |area| %>
		<% area.each  do |libro| %>
		<%= link_to area.nombre, url_for(:controller=>'ppal_estudiante',:action=>'ecatalogo_categorias', :param => area.id) %>(<%= area.libro.count %>) |
		<% end %>
	<% end %>

</div>
