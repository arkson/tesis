<div class = "toplibros">

	<h3><a>Total recaudado: <%= number_to_currency(@recaudado[0].total, :unit => "Bs.") %></a></h3>

	<h3><a>Libros más alquilados</a></h3>
	<div>
		
	<% @cont = 1 %>

	<% @top_libros.each do |lib| %>
		
		<dl>	
		<dd><b> <%=@cont %>. </b> <%= lib.titulo %> - <%= lib.cota %>	(<%=lib.cantidad %>)</dd>
		<% @cont = @cont + 1 %>
		</dl>
	
	<% end %>
		
	</div>

</div>

