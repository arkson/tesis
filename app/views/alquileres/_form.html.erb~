
	<p>
		<b>Datos del Usuario</b>
	</p>

  	<p>
	  <b>Cedula:</b>
	  <%= @alquiler.usuario.cedula %>
	</p>


	<p>
	  <b>Nombre:</b>
	  <%= @alquiler.usuario.nombre %>
	</p>

	


  <p>
 	 <b>Libros seleccionados</b>
  </p>
	
	<table>
	  <tr>
		 <th>Cota</th>
		 <th>Titulo</th>
		 <th>Autor</th>
		 <th>Edicion</th>
		 <th>Año</th>
		 <th>Precio</th>
		 <th>Estatus</th>
		 <th></th>
	  </tr>

		<% @alquiler.line_item.each do |item| %>
			<tr>
			<td><%= item.ejemplar.cota %></td>
			<td><%= item.ejemplar.libro.titulo %></td>
			<td><%= item.ejemplar.libro.autor %></td>
			<td><%= item.ejemplar.libro.edicion %></td>
			<td><%= item.ejemplar.libro.ano %></td>
			<td><%= number_to_currency(item.ejemplar.costo_alquiler) %></td> 
			<td><%= item.estatus_devolucion %><td>
					
			<td><% if Usuario.find(session[:usuario_id]).rol == "Administrador" or Usuario.find(session[:usuario_id]).rol == "Operador"  %>
				<%= button_to 'Devolver', devoluciones_path(:line_item_id => item.id, :alquiler_id => @alquiler.id), :method => :edit %>
			<% end %></	td>	
			
			</tr> 
		<% end %>
		<tr>
			
			<td> </td>
			<td> </td>
			<td> </td>
			<td> </td>
			<td> </td>
			<td> <%= number_to_currency(@alquiler.total_price)  %> </td>
			
		<tr>		  		
	</table>
	
 <% if Usuario.find(session[:usuario_id]).rol == "Estudiante" %>
  <div class="actions">
	<%= button_to "Alquilar", @alquiler, :method => :post %>

  </div>
 <% end %>


